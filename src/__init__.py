# ============================================================================
# STRATAAI - SOURCE PACKAGE
# ============================================================================
# WHY THIS FILE EXISTS:
# __init__.py makes a directory a "Python package" — meaning you can import
# modules from it using dot notation like: from src.pdf.extractor import extract_text
#
# Without this file, Python would NOT recognize 'src' as a package,
# and ALL imports from this directory would fail with ModuleNotFoundError.
#
# This file is intentionally empty for now. As the project grows, we may
# add package-level imports here for convenience.
# ============================================================================
